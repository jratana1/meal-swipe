<%=image_tag("#{@photo.url}", alt: "Yummy Food")%>
<% if @photo.user_id == current_user.id %>
<%= button_to 'Delete Photo', user_photo_path(@photo), method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>